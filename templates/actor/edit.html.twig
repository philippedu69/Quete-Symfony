{% extends 'base.html.twig' %}

{% block title %}Edit Actor{% endblock %}

{% block body %}
    <h1>{{ actor.name }}</h1>

    {{ include('actor/_form.html.twig', {'button_label': 'Save'|trans}) }}

    <a class="btn btn-secondary" href="{{ path('actor_index') }}">{{ 'back to list'|trans }}</a>

    {{ include('actor/_delete_form.html.twig') }}
{% endblock %}
